# Decision Log

Lightweight, chronological. One line per decision. Anything large enough to need alternatives and
consequences becomes an [ADR](adr/) instead.

| Date | Decision | Why | Ref |
|---|---|---|---|
| 2026-08-07 | Reframed "the AI must never hallucinate" to **"no unsourced assertion, and no unverified source"** | A model-behaviour claim is unfalsifiable; a system claim is testable. Citation ≠ correctness | `00-discovery.md` |
| 2026-08-07 | Document acquisition & MPN↔document binding promoted to a **first-class module** | It is the real bottleneck; demoing past it invites the fatal question | `01-requirements.md` FR-DOC |
| 2026-08-07 | Positioning: **margin expansion inside an existing content operation**, not BPO replacement | Judges likely include the content-ops team; "we replace you" is a losing frame | `12-hackathon-strategy.md` |
| 2026-08-07 | Vertical wedge: **PVF / flow control**, 5 classes | Messiest units in industrial → deterministic engineering visibly beats naive AI | `domain/pvf-reference.md` |
| 2026-08-07 | Introduced **attribute risk tiers**; Tier 0 never auto-accepts | A uniform threshold treats pressure ratings like handle types | ADR-0009 |
| 2026-08-07 | North-star metric: **STP rate at ≥98% precision**, reported alongside SKU-level correctness | Per-attribute precision overstates SKU-level correctness (0.98¹³ ≈ 77%) | `01-requirements.md` QR-16 |
| 2026-08-07 | Gold set target raised to **400–600 labelled values**; real and synthetic slices reported separately, real first | Synthetic-only metrics are inflated and a domain judge will find it | `09-testing.md` |
| 2026-08-07 | **Prompt injection elevated to a core requirement** (INV-7) with an adversarial CI slice | Our input surface is third-party documents; on-brand security work for a trust product | `08-security.md` |
| 2026-08-07 | Stack: Python/FastAPI + Next.js + Postgres | Team strengths; PDF/ML ecosystem; one datastore does five jobs | ADR-0003 |
| 2026-08-07 | Invariants enforced as **database CHECK constraints** and **import-graph tests**, not code review | A guarantee that depends on remembering is not a guarantee | `04-data-model.md`, `05-backend.md` |
| 2026-08-07 | **Judge Mode** included in Track A | Highest impact-per-hour item for a domain-expert audience | FR-JDG |
| 2026-08-07 | Description generation explicitly **out of scope** | Contradicts the thesis; refusing it is itself a positioning statement | OOS-3 |
| 2026-08-07 | **Eval harness lands at M1**, before the extractor | Otherwise three weeks of tuning happen blind | `10-roadmap.md` |
| 2026-08-07 | `demo` treated as a first-class environment with a versioned snapshot | Demo-day failure is the highest-consequence, most-preventable risk | `07-devops.md` |
| 2026-08-07 | Track A frozen after M0; additions require a removal | Scope creep is the dominant failure mode of ambitious 4-week builds | `10-roadmap.md` |
| 2026-08-13 | Backend foundation slice built against an in-memory `RecordRepository` adapter; Postgres schema designed (DDL-verified) but not run | No Docker/Postgres available in the build environment; §14's SQLite rejection stands, so the dev/test adapter is in-memory, not SQLite | `15-backend-implementation-status.md` |
| 2026-08-13 | Reconciled the design docs against the actual competition brief and ground-truth files (Delivery Format CSV, Input CSV, LOV/manufacturer/UOM reference pack) | These weren't available when `00`–`15` were written; the brief scores a 252-column deliverable and a client-supplied vocabulary, not the PVF/ball-valve demo scope originally assumed | `16-unilog-alignment.md` |
| 2026-08-13 | Description generation reversed from out-of-scope (OOS-3) to in-scope, templated only | The brief calls multi-format description construction "most of the task"; templating from already-verified `AttributeValue`s doesn't reintroduce the fabrication risk OOS-3 was written to avoid | ADR-0013 |
| 2026-08-13 | Demo classes re-scoped from the hand-authored PVF taxonomy to the client's Fittings + Faucets categories | Both are specified end-to-end by the client with worked ground truth; the hand-authored classes have none to score against | ADR-0014 |
| 2026-08-13 | `Evidence` widened from PDF-span-only to a tagged union (document span / source-row span / reference-table row) | Most ground-truth values in this dataset come from the input row or a reference table, not a manufacturer PDF; INV-1 as originally typed made those values unconstructable | `16-unilog-alignment.md` G1 |
| 2026-08-13 | New execution track (UH0–UH7) defined ahead of `10-roadmap.md` M1–M6, without renumbering that file | M0's architectural deliverables are already substantially built; the remaining work reorders around the competition's real scoring target | `16-unilog-alignment.md` §5 |
| 2026-08-13 | `RES` (manufacturer/brand resolution) output modelled as `AttributeValue` (`MANUFACTURER_NAME`/`BRAND_NAME`, risk_tier=1), not a bespoke result type | The existing sealed union already expresses exactly the states RES needs (ACCEPTED/NEEDS_REVIEW/UNKNOWN) and already enforces INV-1/INV-2/INV-5 structurally; a parallel type would duplicate that enforcement, not add anything | `16-unilog-alignment.md` UH2 |
| 2026-08-13 | `RES` built and fully tested against the resolution *architecture* (normalisation, indexed lookup, exact/normalized/alias/fuzzy tiers, ambiguity handling) with a real workbook still missing; real accuracy deferred | Re-verified machine-wide at the start of this session — `UniCat_Manufacturer_and_Brand_List.xlsx` is not present; fabricating it or its match results was out of scope. Descriptive corpus stats computed from the real `sample_input.csv` instead (normalisation clusters, embedded-code extraction, cross-field near-miss conflicts) | `15-backend-implementation-status.md` §9 |
| 2026-08-14 | `SCH` (taxonomy/LOV cutover) built as architecture + typed loaders + declarative category-scope config with all three LOV workbooks still missing; `category_scope.yaml` shipped with an empty rule list rather than invented Classpath prefixes | Re-verified machine-wide (4th pass) at the start of the UH3 session — none of `Unicat_Lov_v1_0...xlsx`, `Fittings_LOV.xlsx`, `FAUCETS_LOV.xlsx` present; `sample_input.csv` has no `Classpath` column either, so real rows cannot be scoped to Fittings/Faucets in this environment at all | `16-unilog-alignment.md` UH3, `15-backend-implementation-status.md` §10 |
| 2026-08-14 | `EXT`/`VER` shipped a real (not fixture-only) verbatim source-row extraction path (`MFG_PART_NUM`/`ITEM_DESCRIPTION`, exact-match verified) against all 1,000 real `sample_input.csv` rows; class-specific Fittings attribute extraction stayed architecture-only | Verbatim row-field extraction needs no LOV to verify (the citation *is* the value); class-specific extraction does need one, and asserting an unverifiable value would violate INV-2. NPS↔DN equivalence table shipped empty — OD-4 (primary-source verification) is still open, so no unverified numbers were hardcoded | `16-unilog-alignment.md` UH4, `15-backend-implementation-status.md` §11 |
| 2026-08-14 | `DSC` formula engine + validation architecture built and tested; `resources/description-formulas/` shipped with zero class formula files, only two confirmed field constraints (`INVOICE_DESC`, `MOBILE_DESC`) | `UNILOG_INTERNAL_CONTENT_GUIDELINES.docx` still missing (5th verification pass) — the only source for the other three fields' construction rules and any field's numeric limit beyond what ADR-0013's own worked example already states. A missing class formula file is treated as "not configured yet" (soft), unlike client reference workbooks, because it's this project's own not-yet-written config | `16-unilog-alignment.md` UH5, `15-backend-implementation-status.md` §12 |
| 2026-08-14 | Frontend's default API base stayed pointed at the mock fixture universe rather than the real backend, despite UH6 asking for the switch | The real backend serves 2 unrelated demo records with no write path for any real pipeline output; switching the default would make the demo strictly worse, not better, and contradicts the standing "preserve mock mode" instruction. Contract compatibility was verified instead (no drift since UH1/UH2) | `16-unilog-alignment.md` UH6, `15-backend-implementation-status.md` §13 |
| 2026-08-14 | `ExportTarget`/Delivery Format projection built and run against real data (1,000/1,000 rows, 252/252 columns, zero structural validation failures); hosted-Postgres deployment and a live prototype link were not attempted | The export architecture needed no missing file to build or exercise for real. Deployment needs external hosted infrastructure (account creation, credentials) this session cannot create unilaterally — a genuine environment blocker, not faked with a placeholder URL | `16-unilog-alignment.md` UH7, `15-backend-implementation-status.md` §14 |
| 2026-08-14 | Returned to the original `10-roadmap.md` M0–M6 track after UH0–UH7; M0 implemented as a gap-fill against its literal checklist, not rebuilt | UH0–UH7 already satisfied most of M0's architectural intent under different names; re-deriving the true remaining scope from the roadmap's own wording (not from UH's summary of itself) avoided both duplicate work and silently-expanded scope | `10-roadmap.md` M0, `15-backend-implementation-status.md` §15 |
| 2026-08-14 | Alembic migration `0001_initial` generates its DDL from `Base.metadata.create_all`/`drop_all` directly rather than a hand-transcribed sequence of `op.create_table(...)` calls | `infrastructure/db/models.py` is already the single reviewed, tested source of truth for the schema (`test_db_schema.py`); a second, independently-maintained copy in the migration is a drift risk with no offsetting benefit for a migration #1 that has nothing to diff against | `15-backend-implementation-status.md` §15 |
| 2026-08-14 | Fixed a circular foreign-key dependency between `attribute_value.verification_id` and `verification.attribute_value_id` (`use_alter=True`) | Discovered while building the M0 migration: `Base.metadata.sorted_tables` silently dropped both FK constraints from generated DDL rather than erroring, which would have produced a schema missing two documented constraints the first time it ran against a real Postgres | `04-data-model.md` §3.4, `15-backend-implementation-status.md` §15 |
| 2026-08-14 | `ING`'s write path modelled by extending `InMemoryRecordRepository` to also implement a new `ImportBatchRepository` port, rather than a second in-memory store | One process-lifetime instance already satisfying `RecordRepository` (read); adding the write port to the same class means a record `ING` just ingested is visible to the very next `GET /records` call without a second store to keep in sync | `15-backend-implementation-status.md` §15 |
| 2026-08-14 | M0's "Corpus fetch scripts... 150+ documents" and "Gold set v1: 150 labelled attribute values" deliverables left unimplemented | Both target the original hand-authored PVF corpus, which ADR-0014/`16-unilog-alignment.md` already demoted to an architecture-test fixture with no ground truth to score against; the UniHack pivot's own equivalent data (manufacturer PDFs, a real labelled gold set) has been independently confirmed absent from this environment across five-plus verification passes in UH0–UH7. Not a decision to skip — a standing, documented environment/data blocker | `10-roadmap.md` M0, `15-backend-implementation-status.md` §7, §15 |

| 2026-08-14 | Returned to `docs/10-roadmap.md`'s original M1 scope (CLS + SCH + EVL) rather than continuing UH-track numbering | `docs/16-unilog-alignment.md` §5 already noted M0's architectural deliverables were substantially done by UH0–UH7; this session's brief explicitly asked for the roadmap's own M1, picked up where UH7/M0 left off | `10-roadmap.md` M1, `15-backend-implementation-status.md` §16 |
| 2026-08-14 | `CLS`'s deterministic pre-pass targets the one real class already loaded (`BALL_VALVE_BRONZE`, ADR-0011's fixture) using an abbreviation dictionary transcribed verbatim from `docs/domain/pvf-reference.md` §8; Fittings/Faucets category-level classification rules were not added | The abbreviation dictionary is OpenSpec's own reviewed domain documentation, not client data, so it can be exercised for real; the client's real classification vocabulary still needs the missing Unicat LOV, and `classify_record` is deliberately class-set-agnostic so no code changes when that source arrives | `15-backend-implementation-status.md` §16 |
| 2026-08-14 | An LLM-proposed class's confidence is a fixed, policy-configured constant (`ClassificationPolicy.llm_validated_confidence`) assigned only after taxonomy validation passes, never the model's own self-reported confidence | CLAUDE.md: "Confidence is a calibrated composite of measured signals, never a model self-report" — the only measured signal available at classification time is "did this survive taxonomy validation" | `15-backend-implementation-status.md` §16 |
| 2026-08-14 | `Completeness` (the `GET /records/{id}` `completeness` object) and its computation moved from a private, untested helper in `infrastructure/memory/repositories.py` to `domain/sch/completeness.py` | Pure computation over already-materialised `AttributeValue`s belongs in `domain/`, not embedded in a dev-fixture repository adapter; `docs/10-roadmap.md` M1 lists "completeness computation" as a SCH deliverable explicitly | `15-backend-implementation-status.md` §16 |
| 2026-08-14 | `EVL`'s gold-set contract, validation, metrics, and result model were built and fully tested against small fixtures, then wired for real against the existing UH4 pipeline's predictions over all 1,000 `sample_input.csv` rows; no real gold set exists to score against, so `make eval` honestly reports `GOLD_SET_UNAVAILABLE` | Re-verified machine-wide (sixth pass) at the start of this session — no gold set, no LOV workbooks, no new reference files found anywhere. Building the harness now and pointing it at real predictions means it needs zero rework the moment a real gold set is supplied, per `docs/10-roadmap.md` M1's own "EVL must exist before the extractor" sequencing rationale | `16-unilog-alignment.md` G7, `15-backend-implementation-status.md` §16 |

| 2026-08-14 | `DocumentBinding.status=ACCEPTED` is structurally gated to `EXACT_MPN`/`NORMALIZED_MPN`/`HUMAN` methods only (a `__post_init__` guard, `domain/model/document.py`) | M2 brief: "do not allow a low-confidence match to become an asserted binding automatically" — the same "fabrication is unrepresentable" discipline INV-9 already applies to Tier-0 `AttributeValue`s, applied to binding confidence instead of attribute risk | `10-roadmap.md` M2, `domain/model/document.py` |
| 2026-08-14 | DOC's retrieval hierarchy (exact → normalised → supplier → class → overlap → LLM disambiguation) implemented as a narrowing cascade, mirroring UH2's `resolve_manufacturer_brand` tiered-resolution shape exactly | The two problems are structurally identical ("pick the one right thing from a candidate pool using progressively weaker deterministic signals, escalate to AI only for genuine ties"); reusing a proven pattern instead of inventing a second one keeps the codebase's decision-cascade vocabulary consistent | `10-roadmap.md` M2, `domain/doc/binding_engine.py` |
| 2026-08-14 | `DocumentRepository` (read) / `DocumentIngestRepository` (write) / `BindingRepository` split into three ports, one `InMemoryDocumentRepository` implementing all three | Mirrors the existing `RecordRepository`/`ImportBatchRepository` split (`application/ports/repositories.py`, `import_repository.py`) and its "one process-lifetime store, several port roles" precedent — a new pattern would be duplicate architecture for the same problem | `10-roadmap.md` M2, `application/ports/document_repository.py` |
| 2026-08-14 | The M2 backend's document corpus starts genuinely empty (`InMemoryDocumentRepository()` with no seed data) rather than mirroring the frontend mock's ~18-document fixture universe | M2 brief: "Do not fabricate a PDF/document corpus... Synthetic fixtures must never become production/demo data accidentally." The real backend only ever holds what `POST /documents` actually ingests — an honest `GET /documents` → `[]` until a real upload happens, not a second invented dataset | `10-roadmap.md` M2, `infrastructure/memory/document_repository.py` |
| 2026-08-14 | `PdfplumberParser`/`Pypdfium2Rasterizer` (real adapters, per ADR-0005/ADR-0012) validated against a hand-built, byte-exact synthetic PDF (`tests/fixtures/pdf/minimal_pdf.py`) rather than mocked or left untested, including a real stroked-grid fixture that pdfplumber's own table detector genuinely recognises | The real manufacturer-document corpus is confirmed absent (unchanged since UH0); building one small hand-crafted PDF is not "fabricating a corpus" — it proves the adapter parses real PDF byte structure, which a mock cannot prove, while never being presented as real-world accuracy | `10-roadmap.md` M2, `tests/fixtures/pdf/minimal_pdf.py` |
| 2026-08-14 | OCR fallback defaults to `UnavailableOcrProvider`; `TesseractOcrProvider` is real, working code but not the composition-root default | No `tesseract` binary is installed in this sandbox (verified this session) — defaulting to a provider that would silently fail is worse than an honest `OcrUnavailable` → `ParseFailed(NO_TEXT_LAYER_OCR_UNAVAILABLE)`, the same `LLMProvider`-`offline`-mode discipline this project already applies elsewhere | `10-roadmap.md` M2, `infrastructure/parsing/ocr.py` |

## Open decisions

| # | Question | Blocks | Owner | Due |
|---|---|---|---|---|
| OD-1 | Confirm assumption A1 (UniHack = Unilog; judges are domain experts) | Positioning only | — | Week 1 |
| OD-2 | Obtain the real CX1 import schema / API docs | ADR-0010 mapping, M6 export | — | Day 5 — **partially resolved**: the Delivery Format CSV is a confirmed 252-column export target even though the live CX1 API schema remains unconfirmed (`16-unilog-alignment.md` G8) |
| OD-3 | ETIM licensing terms — adopt or stay hand-authored | ADR-0011, TD-4, now also ADR-0014 | — | Week 2 |
| OD-4 | Verify PVF domain rules (§4–§7 of `domain/pvf-reference.md`) against primary sources | All validation rules | — | Week 1 |
| OD-5 | Manual-baseline throughput figure for the cost comparison | Business slide | — | M5 |
| OD-6 | Obtain `UniCat_Manufacturer_and_Brand_List.xlsx` and the rest of the UH0 reference pack (`Unicat_Lov_v1_0...`, `Fittings_LOV.xlsx`, `FAUCETS_LOV.xlsx`, UOM standards, `Decimal_Fraction.xlsx`, content guidelines) | UH2 real accuracy, UH3, UH4's `NRM`, UH5 — re-verified missing three separate sessions (`15-backend-implementation-status.md` §7, §9) | — | Blocking |
| OD-7 | Obtain a real manufacturer-document corpus (150+ PDFs) and a labelled gold set (150+ attribute values) for M0's own checklist items | `10-roadmap.md` M0's "Corpus fetch scripts" and "Gold set v1" deliverables, and everything downstream that gates on the gold set (§4's risk register R4/R14) — now also blocks `EVL` reporting real accuracy (M1) **and M2's own verification checklist** ("binding accuracy ≥95%", "adversarial wrong-document rejection ≥90%", "parse cache hit ratio >95% on a same-document batch") **and M3's real class-specific extraction/verification/validation accuracy** — all real-corpus-shaped, all unreachable in this environment (re-verified `15-backend-implementation-status.md` §18 — eighth pass) | — | Blocking (re-verified `15-backend-implementation-status.md` §15, §16, §17, §18 — eighth pass) |

| 2026-08-14 | `EXT`/`VER`'s structured LLM output schemas (`ExtractorProposalPayload`, `VerifierVerdictPayload`) are hand-written frozen dataclasses with manual `json.loads` + closed-key-set validation, not `pydantic.BaseModel`, despite `pydantic` being architecturally allowed in `domain/` | Tried `BaseModel` first; it fails `pyproject.toml`'s `disallow_any_explicit` override for `domain/`/`application/` even with the `pydantic.mypy` plugin enabled (tried and reverted — no change) — a real, checked incompatibility, not a style preference. Hand-written validation gives the identical guarantee and matches every other domain type's existing construction style | `15-backend-implementation-status.md` §18, `domain/ver/llm_verdict.py`, `domain/ext/llm_proposal.py` |
| 2026-08-14 | `VAL`'s rule engine is a genuinely restricted expression DSL (`Field`/`Compare`/`BoolExpr`, parsed from YAML data shapes) rather than a fixed set of named rule "kinds" | `docs/05-backend.md` §6 specifies "comparison, boolean logic, arithmetic, and field references", not a template library — a real interpreter over structured, non-`eval()`'d data satisfies that literally while staying provably safe (`tests/architecture/test_no_eval.py` covers it, no new exemption needed) | `docs/10-roadmap.md` M3, `domain/val/rules_dsl.py` |
| 2026-08-14 | `resources/rules/ball_valve_bronze.yaml` ships 18 rules, not the ≥60 `docs/10-roadmap.md` M3 targets | `docs/domain/pvf-reference.md` §10 names exactly 12 rule IDs; this file expands two per pressure attribute and one per enum attribute the loaded taxonomy actually has, reaching 18, all traceable to a real citation. Reaching 60 needs either the other four PVF classes' schemas (not loaded here) or a richer, primary-source-verified rule catalogue than §10 currently provides (OD-4, still open since 2026-08-07) — inventing 42 more rules to hit the number would be exactly the fabricated-business-rules failure mode M3 §11 forbids | `docs/domain/pvf-reference.md` §10, `15-backend-implementation-status.md` §18 |
| 2026-08-14 | `EXT` candidates are verbatim by construction in this milestone — `value_raw` is always exactly the cited evidence snippet, never a silently-interpreted or unit-converted form | `docs/10-roadmap.md` M3 §2: "EXT should preserve the source truth. Normalization belongs later." This also makes exact-match the correct, sufficient deterministic entailment check for every M3 candidate, not a simplification specific to one attribute — `domain/ver/independent_check.py` | `docs/10-roadmap.md` M3, `domain/ext/candidate_builder.py` |
| 2026-08-14 | `EXT`/`VER`/`VAL` were proven for real against the `BALL_VALVE_BRONZE` architecture-test fixture and explicitly-labelled test fixtures, not against real `sample_input.csv` PVF attributes | `sample_input.csv`'s real 1,000 rows are generic industrial-supply items (adhesives, tools, electrical parts), not PVF product data — re-verified by inspection this session. Attempting PVF-specific attribute extraction against it would risk spurious pattern matches with no ground truth to check them against, exactly the failure mode this project's own thesis exists to prevent. UH4's real, live, evidenced `MFG_PART_NUM`/`ITEM_DESCRIPTION` path against all 1,000 real rows is untouched by this milestone | `15-backend-implementation-status.md` §18, `docs/16-unilog-alignment.md` UH4 |
