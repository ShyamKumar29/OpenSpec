"""Pipeline stages (docs/05-backend.md folder structure). One file per
stage, mirroring the module codes in CLAUDE.md. `ext.py` (UH4) and `ing.py`
(docs/10-roadmap.md M0) exist so far — the rest are built as later milestones
need them, per this session's "implement only what a milestone actually
needs" discipline.
"""
