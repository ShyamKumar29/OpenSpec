"""`VAL` — declarative validation (M3, `docs/10-roadmap.md` M3). Consumes already
extracted/verified `AttributeValue`s (via caller-supplied `Facts`, `domain/val/
rules_dsl.py`); it never extracts a value itself (`docs/10-roadmap.md` M3 §14).
"""

from __future__ import annotations
