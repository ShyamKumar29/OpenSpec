"""`EVL` — evaluation harness (M1, docs/10-roadmap.md M1: "EVL must exist
before the extractor"). Pure gold-set validation (`gold_validation.py`) and
metric computation (`metrics.py`) — no I/O, no pipeline mutation (INV-6;
M1 brief: "Keep evaluation separate from the production pipeline").
"""
