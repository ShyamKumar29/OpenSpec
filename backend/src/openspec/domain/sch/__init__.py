"""`SCH` — pure schema-resolution logic (UH3, docs/16-unilog-alignment.md).

INV-6: no I/O here, same discipline as domain/val and domain/nrm — this
package is intentionally not added to that architecture test's banned-import
list because it imports nothing beyond what domain/model already allows.
"""
