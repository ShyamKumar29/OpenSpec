"""PRS — parsing (`docs/10-roadmap.md` M2). Pure: region-path addressing, parse
cache keys, and the typed parse outcome. No PDF library is named here — that lives
behind `application/ports/parser.py` in `infrastructure/parsing/`.
"""
